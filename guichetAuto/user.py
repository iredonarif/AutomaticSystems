class User(object):

	def __init__(self, id, lastName, firstName, balance, code, createdAt):
		self.id = id
		self.lastName = lastName
		self.firstName = firstName
		self.balance = balance
		self.code = code
		self.createdAt = createdAt
		